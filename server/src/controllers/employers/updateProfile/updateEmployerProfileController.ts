import { Response } from 'express';

import { UpdateEmployerRequest } from './types';

export const updateEmployerProfileController = async (
  req: UpdateEmployerRequest,
  res: Response,
) => {
  //pass
};
